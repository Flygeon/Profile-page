name: Deploy Profiles Site
on:
  repository_dispatch:
    types: [deploy-profiles]  # 监听与桥梁工作流中 event_type 一致的事件

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout profiles repository
        uses: actions/checkout@v4

      - name: 执行部署
        # 在这里添加您实际的部署步骤
        # 例如：安装依赖、构建、部署到服务器等
        run: |
          echo "开始部署 profiles 仓库..."
          # 您的实际部署命令
          # npm install
          # npm run build
          # 部署命令...
